/**
 * Created by PavlinGanchev on 8/7/2015.
 *
 * This script is created only for scroll to top button
 */

/**
 * calculate where the button have to appear
 */
$(window).scroll(function(){
    if ($(this).scrollTop() > 310) {
        $('.scrollUpContainer').fadeIn('slow');
    } else {
        $('.scrollUpContainer').fadeOut('slow');
    }
});

/**
* Button action on click
*/
$('.scrollUp').click(function(){
    $('html, body').animate({scrollTop : '0px'}, 'slow');
   return false;
});